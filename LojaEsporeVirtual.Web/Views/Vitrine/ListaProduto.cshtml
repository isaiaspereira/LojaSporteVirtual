@using LojaEsporeVirtual.Web.Helps
@model LojaEsporeVirtual.Web.Models.ProdutosViewModel


@foreach (var item in Model.Produtos)
{
    @Html.Partial("ProdutoDetalhes",item)
}

   @Html.PageLinks(Model.Paginacao, x => Url.Action("ListaProduto", new { pagina = x, categoria=Model.CategoriaAtual }))
